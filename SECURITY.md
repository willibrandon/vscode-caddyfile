# Security

Report extension vulnerabilities through GitHub private vulnerability reporting. Do not open a
public issue for a suspected vulnerability.

The extension does not download Caddy, contact the Caddy admin API, start servers, or run installed
commands in untrusted workspaces. Optional Caddy checks are disabled by default.

Caddy server vulnerabilities belong to the Caddy project and should follow its security policy.
